window.addEventListener('load', () => {
    inputKeyupHandler('username');
    inputKeyupHandler('password');

    inputClickHandler('username');
    inputClickHandler('password');
        
    inputBlurHandler('username');
    inputBlurHandler('password'); 
});